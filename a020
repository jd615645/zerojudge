#include <stdio.h>

int main()
{
    char id[10];
    int tot,cheak,i,j,eng;
    int chg[26]={10,11,12,13,14,15,16,17,34,18,19,20,21,22,35,23,24,25,26,27,28,29,32,30,31,33};
    while(scanf("%s",&id) != EOF)
    {
        tot=0;
        if(id[0]>='a' && id[0]<='z')
        {
            id[0]-='a'-'A';
        }

        eng=chg[(int)id[0]-65];

        tot=eng / 10 + (eng%10)*9;

        for(i=1,j=8;i<=8;i++,j--)
        {
            tot+=( (id[i]-'0') *j);
        }
        tot+=( (id[9]-'0'));

        printf("%s\n",tot%10==0?"real":"fake");
    }

    return 0;
}
