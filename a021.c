#include <stdio.h>
#include <string.h>

int main()
{
	char num_1[500],num_2[500],sign,ans[500];
	int i;
	while(scanf("%s %s %s",num_1,sign,num_2) != EOF)
	{
		for(i=0;i<=strlen())
		
		if(sign=='+')
		{
			
		}
		else if(sign=='-')
		{
			
		}
		else if(sign=='*')
		{
			
		}
		else if(sign=='/')
		{
			
		}
		
		printf("%d\n",n);
	}
		
	return 0;
}
